from pathlib import Path


def extract_keyframes(_video_path: Path, _output_dir: Path) -> list[Path]:
    raise NotImplementedError(
        "keyframe extraction is not implemented in the initialization scaffold"
    )
