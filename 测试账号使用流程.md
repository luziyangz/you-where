# 小程序测试账号与测试流程

## 测试账号

项目已内置隐藏测试用户种子脚本，后续部署到远程服务器后，需要在服务器真实生产环境执行脚本，将两个测试用户写入生产 MySQL 的 `users` 表。

- 写入脚本：`backend/scripts/seed_test_users.py`

| 角色 | open_id | 昵称 | 共读码 | 当前状态 |
| --- | --- | --- | --- | --- |
| 测试用户 A | `youzainaye_test_user_a` | `测试用户A` | `900001` | 写入后可绑定 |
| 测试用户 B | `youzainaye_test_user_b` | `测试用户B` | `900002` | 写入后可绑定 |
| 微信审核用户 | `youzainaye_review_user` | `微信审核员` | `900003` | 审核登录后自动写入 |

这两个账号写入的是正式业务表 `users`，绑定时会进入正常的 `pairs`、`active_pair_locks` 链路。它们不会在小程序界面中展示，也不会出现独立的测试登录按钮。

## 生产环境写入方式

部署到远程服务器后，进入后端部署目录执行：

```bash
cd /opt/you-where-backend
sudo docker compose exec backend python scripts/seed_test_users.py
```

执行成功后会输出类似：

```text
Seeded hidden test users:
- A: user_id=... open_id=youzainaye_test_user_a join_code=900001
- B: user_id=... open_id=youzainaye_test_user_b join_code=900002
```

如果你不是用 Docker Compose 部署，而是在服务器直接运行 Python 后端，需要先确认 `.env` 中的生产数据库配置正确，然后执行：

```powershell
cd backend
python scripts\seed_test_users.py
```

脚本是幂等的：重复执行会补齐/更新这两个测试用户，不会重复创建多份账号。

如测试账号已经被绑定，想重新测试绑定流程，可在服务器执行：

```bash
cd /opt/you-where-backend
sudo docker compose exec backend python scripts/seed_test_users.py --reset-active-pairs
```

非 Docker 部署则执行：

```powershell
cd backend
python scripts\seed_test_users.py --reset-active-pairs
```

`--reset-active-pairs` 会把涉及测试用户 A/B 的活跃共读关系置为解绑状态，便于重复测试绑定流程。生产环境只在需要重复测试时使用。

## 微信审核账号密码

微信提审时，如果页面要求填写测试账号和密码，使用审核专用账号，不要填写真实微信账号密码。

服务器 `.env` 需要配置：

```env
ENABLE_REVIEW_LOGIN=1
WECHAT_REVIEW_ACCOUNT=reviewer
WECHAT_REVIEW_PASSWORD=请设置一个仅供审核使用的密码
```

微信后台提审信息填写：

```text
测试账号：reviewer
测试密码：填写服务器 WECHAT_REVIEW_PASSWORD 的值
```

审核人员在体验版小程序中完成隐私/协议勾选后，点击“审核测试登录”，输入上面的账号密码即可进入完整小程序流程。该入口仅在开发版/体验版展示，正式版隐藏。首次登录会自动在生产数据库写入审核用户 `youzainaye_review_user`，固定共读码为 `900003`。

## 测试人员流程：用真实微信账号绑定测试用户

适合产品、验收、外部测试人员使用，不需要开放测试登录接口。

1. 打开微信开发工具或体验版小程序。
2. 使用自己的微信身份正常登录，并完成隐私协议/用户协议确认。
3. 进入“我的”或“结伴/共读码”相关入口，确认当前账号能看到自己的共读码。
4. 进入绑定伙伴流程。
5. 输入测试用户 B 的共读码：`900002`。
6. 绑定成功后，检查首页/我的页是否显示伙伴昵称 `测试用户B`。
7. 进入书房，选择一本书加入共读。
8. 进入阅读页，新增一条阅读记录或摘录。
9. 回到“我们的记录”，确认记录能展示，并且共读关系、当前书籍、进度数据正常。
10. 如需重复测试绑定，可让开发执行上面的 `--reset-active-pairs` 命令后再试。

也可以输入测试用户 A 的共读码 `900001`，但同一测试用户同一时间只能被一个账号绑定。

## 开发人员流程：直接登录测试用户 A/B

适合联调接口、双账号对照测试。后端必须显式开启：

```env
ENABLE_TEST_USERS=1
```

测试登录接口：

```http
POST /api/v2/auth/test-login
Content-Type: application/json

{"role":"a"}
```

返回后端 session 后，将 `token` 和 `user` 写入小程序本地缓存，即可模拟该测试用户登录。微信开发工具 Console 示例：

```javascript
wx.request({
  url: 'https://www.nizaina.online/api/v2/auth/test-login',
  method: 'POST',
  data: { role: 'a' },
  success(res) {
    const data = res.data.data;
    wx.setStorageSync('token', data.token);
    wx.setStorageSync('user', data.user);
    getApp().setSession(data);
    wx.reLaunch({ url: '/pages/home/index' });
  }
});
```

登录测试用户 B 时，把 `role: 'a'` 改为 `role: 'b'`。

注意：生产环境默认应保持 `ENABLE_TEST_USERS=0`。云端开发通常只需要写入隐藏测试用户，然后让真实微信账号输入 `900001` 或 `900002` 进行绑定测试，不建议长期开放 `/auth/test-login`。

## 建议验收点

- 登录后能看到自己的用户信息和共读码。
- 输入 `900001` 或 `900002` 可以绑定对应测试用户。
- 已绑定状态下再次绑定其他人会被拦截。
- 解绑后可以重新绑定。
- 选择书籍后，首页能显示当前共读书。
- 阅读记录、摘录、回复、进度在“我们的记录”中正常展示。
- 退出登录再进入，小程序能从本地缓存恢复登录态；token 失效时能提示重新登录。
