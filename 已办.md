# 已办事项

## 2026-04-26
- [x] 完成仓库首轮盘点，确认项目为“原生微信小程序前端 + FastAPI 后端”的双端结构
- [x] 识别出当前主入口：
  - 小程序：`app.js`、`app.json`
  - 后端：`backend/app_main.py`
- [x] 确认首批结构风险：
  - `backend/service`、`backend/repo` 为空壳
  - `pages/index`、`pages/logs` 疑似模板残留
  - 协作文档存在编码显示混乱
- [x] 启动产品、开发、测试三个并行审查子角色，开始收敛产品闭环、技术债和测试风险
- [x] 建立团队治理文件：
  - `Agent.md`
  - `team/shared-memory.md`
  - `team/hooks.md`
  - `team/error-log.md`
  - `team/current-plan.md`
  - `team/roles/product.md`
  - `team/roles/development.md`
  - `team/roles/testing.md`
- [x] 重构协作文档基线：
  - `record.md`
  - `待办.md`
  - `已办.md`
- [x] 清理明确未注册的微信模板残留页面：
  - `pages/index`
  - `pages/logs`
- [x] 删除历史二进制噪音文件：`backend/docker-compose-linux-x86_64`
- [x] 修复自定义 Tab 选中态错误：
  - 首页索引修正为 `1`
  - 我的页索引修正为 `3`
- [x] 完成后端回归验证：`pytest test_v2_core_reading.py test_v2_store_reading.py test_v2_e2e_flow.py test_v2_profile.py -q` 结果 `9 passed`
- [x] 修复书城分页逻辑：`/api/v2/store/books?page=N` 现已正确使用分页偏移，不再重复返回第一页数据
- [x] 修复个人统计口径：已解绑关系下仍保留历史完读统计，阅读页数统计不再错误归零
- [x] 补充后端主链负向/边界测试：
  - 自绑与无效共读码拦截
  - 书城分页偏移
  - 锁定笔记不可回复
  - 已解绑关系的历史统计保留
- [x] 修复 `backend/test_v2_store_reading.py` 夹具串数据问题：补充 `Book/Entry` 清理
- [x] 完成第二轮后端回归验证：`pytest test_v2_core_reading.py test_v2_store_reading.py test_v2_e2e_flow.py test_v2_profile.py -q` 结果 `13 passed`
- [x] 确认乱码根因：当前仓库主文本文件大多正常，主要问题是 Windows PowerShell 默认读取方式误读 UTF-8
- [x] 新增 `.editorconfig`，将项目文本编码约束固定为 `UTF-8`
- [x] 更新 `README.md` 编码说明，明确终端读取方式
- [x] 新增功能矩阵文档：`team/feature-matrix.md`
- [x] 为 `create_entry` 增加数据库唯一约束冲突兜底，提升并发幂等稳定性
- [x] 新增并发一致性测试：相同 `client_request_id` 并发重放只生成 1 条记录
- [x] 完成第三轮后端回归验证：`pytest test_v2_core_reading.py test_v2_store_reading.py test_v2_e2e_flow.py test_v2_profile.py -q` 结果 `14 passed`
- [x] 新增 `backend/common/locks.py`，提供命名锁能力
- [x] 为 `pair_bind` 增加并发兜底：用户级命名锁 + `with_for_update()` 复查
- [x] 为 `create_book` 增加并发兜底：`pair_id` 级命名锁 + `with_for_update()` 复查
- [x] 新增绑定并发测试：同一对用户并发绑定只允许 1 个成功
- [x] 新增建书并发测试：同一对伙伴并发建书只允许 1 个成功
- [x] 完成第四轮后端回归验证：`pytest test_v2_core_reading.py test_v2_store_reading.py test_v2_e2e_flow.py test_v2_profile.py -q` 结果 `16 passed`
- [x] 在 `app.js` 建立统一全局状态写入口：`syncUser / syncPair / syncCurrentBook / syncReadingContext`
- [x] `partner` 页改为 `onShow` 刷新，并清理未登录时的陈旧状态
- [x] `partner` 页改为使用 `fetchMe + fetchCurrentPair`，降低对陈旧 `app.globalData.user` 的依赖
- [x] `progress` 页改为通过 `fetchHome` 同步最新 `user / pair / currentBook`
- [x] `home` 页删除绑定、解绑、写笔记逻辑，只保留概览和跳转动作
- [x] `progress` 页成为笔记流、回复和更新进度的唯一详细操作页
- [x] 修复 `partner` 页刷新递归风险：拆出 `updateNavigationState()`
- [x] 完成前端 JS 装载检查：结果 `ok`
- [x] `book-detail` 页新增上下文同步，并在加入共读前主动刷新状态
- [x] `book-detail` 页在“未绑定伙伴/已有在读书”场景下直接给出前置引导
- [x] `reader` 页在同步进度前后刷新 `user / pair / currentBook` 上下文
- [x] 完成扩展后的前端 JS 装载检查：结果 `ok`
- [x] 修复 `partner` 页遗漏 `pair.current_book` 回写的问题，避免已有在读书时 `app.globalData.currentBook` 仍为空
- [x] 增强 `app.js` 的 `syncReadingContext`，支持从 `pair.current_book` 自动同步全局当前书籍
- [x] 为前端状态一致性补充针对性验证脚本：验证 `pair.current_book -> globalData.currentBook` 回写与清空逻辑
- [x] 完成第七轮后端回归验证：`pytest test_v2_core_reading.py test_v2_store_reading.py test_v2_e2e_flow.py test_v2_profile.py -q` 结果 `16 passed`
- [x] 新增数据库活跃配对锁表 `active_pair_locks`，用主键约束防止同一用户多活跃关系
- [x] 新增数据库在读书锁表 `active_book_locks`，用主键约束防止同一配对多本在读书
- [x] 绑定、解绑、建书、双方完读归档链路已接入锁表创建与释放
- [x] 新增前端状态一致性脚本：`node scripts/check_frontend_state.js`
- [x] 书城 Gutendex 外部依赖新增失败计数与轻量熔断，连续失败后跳过外部请求并降级使用本地缓存
- [x] 阅读目标接口返回周期完成度，阅读目标页展示书籍完成和打卡天数进度
- [x] 提醒接口返回 `delivery_status` 与说明文案，提醒页明确当前为配置保存状态
- [x] 完成第八轮后端回归验证：`pytest test_v2_core_reading.py test_v2_store_reading.py test_v2_e2e_flow.py test_v2_profile.py -q` 结果 `18 passed`
- [x] 后端新增手机号登录接口：`POST /api/v2/auth/phone-login`
- [x] 用户模型新增 `phone_number` 字段，并补充已有数据库结构更新脚本
- [x] 前端首页与我的页新增手机号登录按钮，使用 `open-type="getPhoneNumber"`
- [x] 前端新增 `utils/auth-gate.js`，主业务页未登录时统一回首页登录
- [x] 结伴页已绑定状态也展示自己的共读码和共读二维码
- [x] 前端状态脚本新增微信登录、手机号登录、扫码解析和复制码验证
- [x] 完成第九轮后端回归验证：`pytest test_v2_core_reading.py test_v2_store_reading.py test_v2_e2e_flow.py test_v2_profile.py -q` 结果 `19 passed`
- [x] 新增 `utils/qrcode.js`，在小程序端本地生成 QR 矩阵
- [x] 结伴页二维码改为 canvas 绘制，支持预览和保存到相册
- [x] 移除结伴页对 `api.qrserver.com` 外部二维码图片服务的依赖
- [x] 使用 OpenCV 解码验证本地生成二维码可还原绑定协议：`youzainaye://pair/bind?join_code=123456`
- [x] 个人页接入阅读目标进度，展示完成书籍与打卡天数进度条
- [x] 提醒配置接口返回 `delivery_status`、`delivery_message`、`template_id`
- [x] 提醒页保存前接入 `wx.requestSubscribeMessage`，用户拒绝授权时仍保存偏好并提示需授权
- [x] 新增 `reminder_delivery_logs` 每日投递日志表，防止同一天重复推送
- [x] 新增 `backend/scripts/dispatch_reminders.py`，支持按用户时区、提醒时间和微信订阅模板发送提醒
- [x] 更新 MySQL 初始化脚本，补齐阅读目标、提醒配置和提醒投递日志表
- [x] 完成第十一轮前端状态脚本验证：`node scripts/check_frontend_state.js` 结果 `frontend state check ok`
- [x] 完成第十一轮后端回归验证：`pytest test_v2_core_reading.py test_v2_store_reading.py test_v2_e2e_flow.py test_v2_profile.py -q` 结果 `20 passed`
- [x] 验证提醒调度脚本在缺少微信环境变量时安全跳过，不误连外部服务
- [x] 新增 `backend/api/v2/rest_aliases.py`，提供 REST 友好资源路径
- [x] 新增 `/users/me`、`/users/me/profile`、`/users/me/reading-goal`、`/users/me/reminder-config` 等用户资源路径
- [x] 新增 `/pairs/current`、`POST /pairs`、`DELETE /pairs/current` 等配对资源路径
- [x] 新增 `POST /books/{book_id}/entries` 和 `PUT /books/{book_id}/read-mark`，收敛阅读笔记与已读标记路径
- [x] 前端 `services/api.js` 拆分为 `services/api/*` 领域模块，并保持原统一导出兼容
- [x] 新增 `team/api-rest-migration.md`，记录 REST 接口落地矩阵和旧入口替代关系
- [x] 新增 REST 接口回归测试，覆盖用户、配对、阅读、个人资料、目标和提醒
- [x] 完成第十二轮前端模块装载和状态脚本验证
- [x] 完成第十二轮后端回归验证：`pytest test_v2_core_reading.py test_v2_store_reading.py test_v2_e2e_flow.py test_v2_profile.py -q` 结果 `23 passed`
- [x] 新增 `backend/repo/reading_repo.py`，主链 SQLAlchemy 查询下沉到 repo 层
- [x] 新增 `backend/service/reading_service.py`，主链业务规则、并发锁、状态流转下沉到 service 层
- [x] `backend/api/v2/rest_aliases.py` 已改为直接调用 service，不再包装旧 router 函数
- [x] 从对外路由注册中下线 `/me`、`/profile/*`、`/pair/*`、`/entries`、`/books/current` 等旧入口
- [x] `core_reading.py` 对外只保留 auth 类路由，`store_reading.py` 对外只保留书城读取路由
- [x] 新增旧入口 404 验证，确认旧动作式路径不再注册
- [x] 完成第十三轮后端回归验证：`pytest test_v2_core_reading.py test_v2_store_reading.py test_v2_e2e_flow.py test_v2_profile.py -q` 结果 `24 passed`
- [x] 新增 `backend/repo/store_repo.py`，书城数据访问下沉到 repo 层
- [x] 新增 `backend/service/store_service.py`，书城内置种子、Gutendex、熔断、详情评价和阅读分页下沉到 service 层
- [x] `backend/api/v2/store_reading.py` 已瘦身为 3 个书城接口入口
- [x] 前端状态脚本新增 REST 路径断言，防止前端 API 再次误用旧路径
- [x] 新增 `team/user-flow-check.md`，记录小程序正常使用下的功能闭环和页面流转检查
- [x] 完成第十四轮书城专项回归：`pytest test_v2_store_reading.py -q` 结果 `8 passed`
- [x] 完成第十四轮后端全量回归：`pytest test_v2_core_reading.py test_v2_store_reading.py test_v2_e2e_flow.py test_v2_profile.py -q` 结果 `24 passed`
- [x] 第十五轮修复未登录点击底部非首页 tab 时的闪屏和短暂切页问题
- [x] 第十五轮将书城改为本地中文分类书库，覆盖国外名著、历史、心学、玄学术数、中医经络、国学经典等分类
- [x] 第十五轮默认关闭 Gutendex 外部同步，避免本地开发阶段继续混入英文不可加入书籍
- [x] 第十五轮新增 `POST /api/v2/auth/test-login`，内嵌测试用户 A/B，固定共读码为 `900001` / `900002`
- [x] 第十五轮前端状态脚本覆盖未登录 tabbar 拦截、测试用户登录、书城分类接口路径
- [x] 第十五轮验证通过：`node scripts/check_frontend_state.js`、`pytest test_v2_store_reading.py -q` 为 `8 passed`、`pytest -q` 为 `25 passed`
- [x] 第十六轮删除首页和我的页上的测试用户入口，测试用户不再出现在小程序界面
- [x] 第十六轮删除前端 `testLogin` 方法和 `app.loginFlow({ method: 'test' })` 分支
- [x] 第十六轮后端测试用户入口增加 `ENABLE_TEST_USERS` 开关，生产/MySQL 默认关闭
- [x] 第十六轮确认测试用户与真实用户一样写入 `users`、`sessions`，绑定走正式 `pairs` 链路
- [x] 第十六轮验证通过：`node scripts/check_frontend_state.js`、`pytest test_v2_core_reading.py -q` 为 `9 passed`、`pytest -q` 为 `26 passed`
- [x] 第十七轮补齐 `backend/Dockerfile`，后端容器使用国内 apt/pip 镜像并以非 root 用户运行
- [x] 第十七轮补齐 `backend/docker-compose.yml`，编排 MySQL、FastAPI 后端和 Nginx 网关
- [x] 第十七轮新增 Nginx HTTP 网关和 SSL 示例配置，默认联调地址为 `http://47.99.240.126:18080/api/v2`
- [x] 第十七轮新增云端自动部署脚本 `backend/scripts/cloud_deploy.sh`
- [x] 第十七轮新增本地同步脚本 `backend/scripts/sync_to_aliyun.ps1` 和 `backend/scripts/sync_to_aliyun.sh`
- [x] 第十七轮替换 `backend/deploy_guide.md`，明确阿里云安全组、同步部署、SSL 限制和运维命令
- [x] 第十七轮验证通过：PowerShell 脚本语法、Compose YAML 解析、部署包内容检查、`node scripts/check_frontend_state.js`、`pytest -q` 为 `26 passed`
- [x] 第十七轮补充公网健康检查排障：新增 `/nginx-health`，并将云端脚本的本地健康检查与公网自检分开输出
- [x] 第十八轮确认测试账号 A/B 固定共读码为 `900001` / `900002`
- [x] 第十八轮修复结伴页二维码显示异常：统一 canvas 显示与绘制尺寸，预览/保存导出为 `512x512`
- [x] 第十八轮简化共读码复制入口：删除文字“复制”，只保留右上角复制图标
- [x] 第十八轮验证通过：`node scripts/check_frontend_state.js`、`pytest test_v2_core_reading.py -q` 为 `9 passed`、`pytest -q` 为 `26 passed`
- [x] 第十九轮新增 `backend/scripts/seed_test_users.py`，支持把隐藏测试用户 A/B 显式写入真实 `users` 表
- [x] 第十九轮支持 `--reset-active-pairs`，用于重复绑定测试时释放测试用户已有活跃关系
- [x] 第十九轮新增 `SEED_TEST_USERS` 环境变量，允许云端开发环境启动时自动补齐测试用户
- [x] 第十九轮新增真实用户绑定已种子测试用户 B 的回归测试
- [x] 第十九轮验证通过：`pytest test_v2_core_reading.py -q` 为 `10 passed`、`pytest -q` 为 `27 passed`、`node scripts/check_frontend_state.js` 通过
