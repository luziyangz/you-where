# 对话记录 (Record)

## 2026-04-25
- **用户**: 询问阿里云轻量服务器部署 FastAPI 后端的方法。
- **助手**: 建议使用 Docker 部署，并提供了 `Dockerfile`、`docker-compose.yml` 和 `deploy_guide.md`。
- **用户**: 尝试使用 `scp` 上传文件时遇到连接超时和权限拒绝。
- **助手**: 指出超时是因为使用了内网 IP，建议使用公网 IP；权限问题建议设置 `admin` 密码或调整目录权限。
- **用户**: 询问 `.env` 与 `docker-compose.yml` 的对应关系。
- **助手**: 解释了环境变量的传递机制，并提供了具体的配置建议。
- **用户**: 要求解释 `docker-compose.yml` 的原理和作用。
- **助手**: 详细解释了容器编排、网络隔离和卷挂载等核心概念。
- **用户**: 询问如何查看 Docker 的 `systemctl` 状态。
- **助手**: 提供了 `systemctl status docker` 等常用命令。
- **用户**: 安装 Docker Compose 时报错。
- **助手**: 提供了国内加速镜像、`pip` 安装以及最终成功的 `yum` 插件安装方案。
- **用户**: 数据库连接报错。
- **助手**: 发现是 `cryptography` 依赖缺失和 `MYSQL_HOST` 配置问题，并完成了修复。
- **用户**: 询问小程序连接后台的配置。
- **助手**: 提供了合法域名配置、HTTPS 证书和 8000 端口开放的指导。
- **用户**: 要求创建 record.md、待办.md、已办.md 并自动记录。
- **助手**: 完成记录文件的创建与初始化，并添加核心记忆规则。
- **用户**: 提供备案域名信息，询问如何打通小程序与云服务器。
- **助手**: 正在提供域名解析、SSL 证书配置及小程序后台设置的详细步骤。
- **用户**: 要求以高级开发测试工程师视角完善小程序并搭建测试环境，优先修复业务逻辑后再测试。
- **助手**: 完成后端笔记流分页与查询优化（降低 N+1 查询）、前端进度页展示与“加载更多”优化、生产域名切换，并补充幂等与分页测试，已通过后端测试集。
- **用户**: 要求继续推进。
- **助手**: 新增高并发查询复合索引（模型与 MySQL 初始化脚本同步）、加强分页断言测试，并再次完成后端回归测试通过。
