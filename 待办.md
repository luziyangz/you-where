# 待办事项

## 当前迭代
- [x] 建立团队治理骨架：`Agent.md`、角色规则、共享 memory、hooks、错误记录
- [x] 输出首轮项目治理计划与可行性结论
- [x] 清理明确未接入主流程的模板残留文件和目录
- [ ] 统一协作文档编码与结构，避免继续出现乱码记录
- [x] 执行一轮后端自动化测试，确认首轮清理未破坏主流程
- [x] 梳理前端页面与后端 API 的功能矩阵，作为下一轮改造基线
- [x] 修正首页/结伴页/进度页的职责重叠，形成更清晰的信息架构
- [x] 补齐提醒功能的产品定位：当前先明确为配置保存，真实订阅消息投递另列交付项
- [x] 主链接口落地 `service/repo` 分层，避免 router 继续承载业务逻辑
- [x] 为小程序端补最小可行的接口 mock / 状态流转测试基线
- [x] 为前端状态一致性补最小自动化校验脚本，避免 `app.globalData` 再次被页面局部写坏
- [x] 评估并增强多实例部署下的数据库层约束，补活跃配对锁与在读书锁表
- [x] 制定 REST 接口落地矩阵
- [x] 新增 REST 友好路径，并让前端 API 服务层调用新路径
- [x] 下线旧动作式接口并补充 404 验证
- [x] 将前端 `services/api.js` 拆分为领域模块并保持页面引用兼容
- [x] 首页新增微信一键登录与手机号登录两种入口
- [x] 主业务页新增登录门禁，未登录时回到首页登录
- [x] 结伴页展示自己的共读码、共读二维码，并支持一键复制
- [x] 扫一扫绑定流程补齐二维码协议解析、自绑拦截和自动绑定
- [x] 共读二维码改为小程序本地 canvas 生成，移除外部二维码图片服务依赖
- [x] 补齐微信订阅消息模板配置、前端授权入口、后端调度脚本和每日幂等投递日志
- [x] 将阅读目标进度接入首页或个人页，形成更完整的目标反馈
- [ ] 在生产环境配置真实订阅模板字段并部署调度任务，完成真机真实投递闭环
- [ ] 制定严格 HTTP 状态码渐进方案，评估 `201 Created`、`204 No Content` 引入时机
- [ ] 在微信真机环境验证手机号登录授权弹窗和 `getPhoneNumber` 返回值
- [ ] 在微信真机环境验证本地 canvas 二维码扫码识别和保存相册权限

## 第二优先级
- [x] 梳理前端页面与后端 API 的映射关系，形成功能矩阵
- [x] 主链用户、配对、阅读、目标、提醒逻辑已下沉到 `service/repo`
- [x] 将书城外部依赖逻辑拆到 `store_service/store_repo`
- [x] 补充小程序端关键链路的联调与人工回归清单
- [x] 审查提醒页、阅读历史页、阅读目标页与主链状态的联动一致性
- [ ] 评估并补充限流、幂等、异常处理和并发压测策略
- [x] 在测试 MySQL（`docker-compose.test.yml` 3307）上跑通 `tests_mysql/` 全绿（13 passed）
- [ ] 记录 k6 各阶段 p95（`scripts/run_loadtest_all.ps1`）

## 风险跟踪
- [ ] 评估 `archive` 与历史二进制文件是否需要继续保留在主仓库
- [x] 评估并补充书城外部依赖 `Gutendex` 的缓存、超时、降级与熔断策略

## 2026-04-26 第十五轮新增闭环
- [x] 修复未登录点击底部非首页 tab 时的闪屏和短暂切页问题
- [x] 将书城默认数据收敛为本地中文分类书库
- [x] 确保分类书城进入详情后可加入共读，不再出现本地书籍不存在的主链断点
- [x] 内嵌测试用户 A/B，固定共读码用于本地绑定测试
- [ ] 使用微信开发者工具和真机复测 tabbar、书城分类、扫码绑定、测试用户绑定全流程

## 2026-04-26 第十六轮新增闭环
- [x] 删除小程序界面上的测试用户入口
- [x] 删除前端 `testLogin` 和 `loginFlow({ method: 'test' })` 特殊分支
- [x] 保留后端测试环境入口，并确保测试用户写入真实用户表和会话表
- [x] 增加 `ENABLE_TEST_USERS` 环境开关，生产/MySQL 默认关闭
- [ ] 在后端测试或管理脚本中补充更明确的测试用户创建说明

## 2026-04-26 第十七轮新增闭环
- [x] 补齐后端 Dockerfile，并使用国内 apt/pip 镜像降低构建失败概率
- [x] 补齐 Docker Compose，编排 MySQL、FastAPI 后端和 Nginx 网关
- [x] 补齐 Nginx HTTP 网关配置，并预留 SSL 配置示例
- [x] 新增云端自动部署脚本 `backend/scripts/cloud_deploy.sh`
- [x] 新增本地同步到阿里云脚本 `backend/scripts/sync_to_aliyun.ps1` 和 `backend/scripts/sync_to_aliyun.sh`
- [x] 替换阿里云部署文档，明确 IP+端口、未备案域名和 SSL 限制
- [ ] 提供阿里云 SSH 用户、密码或密钥后执行真实同步部署
- [ ] 在阿里云安全组放行 `18080/tcp`
- [ ] 云端部署后验证 `http://47.99.240.126:18080/health`
- [ ] 正式上线前完成域名备案、HTTPS 证书绑定和微信后台合法域名配置
- [ ] 如果公网仍返回 502，抓取 `you_where_nginx` 和 `you_where_backend` 最新日志定位 upstream 错误

## 2026-04-26 第十八轮新增闭环
- [x] 确认测试账号 A/B 固定共读码
- [x] 修复结伴页二维码 canvas 显示裁切问题
- [x] 共读码区域只保留一个复制图标
- [ ] 在微信开发者工具和真机上复测二维码扫码识别与保存相册

## 2026-04-26 第十九轮新增闭环
- [x] 补齐云端隐藏测试用户种子脚本
- [x] 支持真实用户直接绑定测试用户共读码 `900001` / `900002`
- [x] 增加重复测试时重置测试用户活跃绑定的能力
- [ ] 云端重新构建后执行 `docker compose exec backend python scripts/seed_test_users.py --reset-active-pairs`

## 2026-04-27 产品与 UI 优化准备
- [x] 梳理当前小程序产品定位、核心用户、核心场景和 MVP 边界
- [x] 输出产品需求文档结构：目标、用户故事、功能清单、验收标准、非功能要求
- [x] 梳理页面信息架构：登录、首页、结伴、书城、阅读/进度、我的
- [x] 制定 UI 视觉方向和组件规范，优先覆盖主链页面
- [ ] 每轮产品/UI 计划结束后同步更新 `record.md`、`待办.md`、`已办.md`
- [ ] 基于 `team/PRD.md` 继续细化首页、结伴页、书城页的 UI 改版方案
- [ ] 在 18-24 轮内结束产品讨论，形成 PRD 定稿与 UI 改版执行计划
- [x] 下一轮讨论首页信息架构与第一屏叙事
- [x] 下一轮讨论结伴页信息架构与绑定前/绑定后差异化设计
- [x] 下一轮讨论书城分类、内容策略与书籍卡片
- [x] 下一轮讨论阅读/进度页的双人互动体验
- [x] 下一轮讨论我的页、目标、历史、提醒的辅助定位
- [x] 下一轮讨论新用户引导与登录前体验
- [x] 后续 UI 讨论必须显式检查并避免 AI 味、模板化布局和空泛文案
- [ ] 如产品/UI 讨论需要外部技能，按需加载可信 skill，不为安装而安装
- [x] 下一轮讨论品牌文案语气与产品表达
- [x] 下一轮讨论 UI 视觉方向、色彩、字体、组件规范
- [x] 下一轮讨论核心状态设计：未登录、未绑定、无书、在读、完读、异常
- [x] 下一轮讨论验收标准与测试清单
- [x] 下一轮讨论数据指标与产品效果评估
- [x] 下一轮讨论 MVP 范围冻结与延期清单
- [x] 下一轮进行 PRD 定稿与 UI 改版执行计划
- [x] 开始 UI 实施：建立全局视觉变量和首页 v1
- [x] 首页 v1 完成后执行自动化回归
- [ ] 在微信开发者工具中人工验收首页四种状态：未登录、未绑定、已绑定无书、已绑定有书
- [ ] 真机确认首页字体 fallback、纸感背景、底部 tabbar 遮挡和弹窗输入体验
- [x] 下一轮 UI 实施：结伴页 v1，重构绑定前/绑定后信息架构并降低绑定后二维码视觉权重
- [x] 重设一级 Tab 信息架构，移除 `partner` 一级 Tab，新增 `progress` 为“日记”Tab
- [x] 统一底部 Tab 与首页/书房/结伴/日记的视觉语言
- [x] 修正 `progress` 成为 tab 页面后的跳转方式和跨 tab 打开记录弹窗机制
- [ ] 微信开发者工具验收新 Tab：首页、书房、日记、我的四个一级入口
- [ ] 微信开发者工具验收结伴页作为二级页后的返回路径、扫码绑定、二维码预览/保存
- [ ] 真机验收底部悬浮 Tab 是否遮挡首页、书房列表、日记流底部内容
- [x] 下一轮 UI 实施：我的页基础视觉收敛，避免它继续保留旧工具页风格
- [x] 修复全局按钮默认样式导致的文字不居中问题
- [x] 修复底部悬浮 Tab 与一级 Tab 页面内容重叠的安全留白问题
- [x] 统一主要页面顶部导航栏背景，降低页面与导航栏割裂
- [x] 退出登录后我的页显示统一的未登录界面，并清理 tabbar 状态
- [x] 重排结伴页绑定前布局，分离“让对方绑定我”和“我绑定对方”
- [ ] 微信开发者工具验收我的页登录前、登录后、退出登录后三种状态
- [ ] 真机复查所有按钮文字垂直居中，重点看绑定、扫一扫、保存昵称、退出登录
- [x] 修复书籍详情页“开始阅读 / 加入共读”按钮视觉
- [x] 底部导航栏从文字标识替换为图标
- [x] 我的页顶部去掉突兀“册”印章，改为资产摘要 chip
- [x] 阅读器 v1 支持目录、阅读模式、字号调整、翻页动效、段落划线
- [ ] 微信开发者工具验收 Tab 图标 mask 渲染是否正常
- [ ] 真机验收书籍详情行动卡 loading、禁用和点击区域
- [ ] 真机验收阅读器目录、阅读模式、字号、翻页、段落划线、同步进度
- [ ] 后续评估是否需要后端支持真实章节目录和精确文字划线存储
- [x] 修复书籍详情页行动区双列按钮导致的窄屏溢出风险
- [x] 阅读器支持左右滑翻页
- [x] 阅读器支持左右区域单点翻页、中间单点呼出工具
- [x] 阅读器将段落划线改为长按触发，避免和单点翻页冲突
- [x] 阅读器目录改为左侧抽屉，非阅读功能默认隐藏
- [x] 调整主要页面顶部留白，降低原生导航栏挤压内容的问题
- [ ] 真机验收阅读器手势冲突：左右滑、左右单点、中间单点、长按划线
- [ ] 微信开发者工具验收书籍详情页顶部和行动区是否完整显示
- [x] 书籍详情页 `加入共读` 和 `开始阅读` 恢复为同一行等宽按钮，并处理窄屏溢出
- [x] 阅读器改为微信读书式上下浮层，默认隐藏非正文工具
- [x] 阅读器底部浮层集中展示进度、上一页、同步进度、下一页、目录、设置
- [ ] 微信开发者工具检查书籍详情同行按钮在 375px/360px 宽度下是否不溢出
- [ ] 真机检查阅读器底部浮层是否遮挡正文最后几行

## 2026-05-07 多代理协作初始化
- [ ] 由 Leader 发布首张任务卡（前端/后端/分析三线并行）
- [ ] 在 Cursor 中按模型分配启动前端 Gemini 子代理
- [ ] 在 Cursor 中按模型分配启动后端 Sonnet4.6 子代理
- [ ] 启动 Auto 分析子代理执行风险审查与回归检查
- [ ] 建立每轮统一汇报节奏：仅汇报结果、不共享上下文
- [ ] 每轮结束同步全局与各代理 `待办.md` / `已办.md`
