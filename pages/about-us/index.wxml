<view class="page">
  <view class="card">
    <view class="title">关于我们</view>
    <view class="subtitle">让共读变得更简单</view>

    <view wx:for="{{sections}}" wx:key="id" class="section">
      <view class="section-title">{{item.title}}</view>
      <view class="paragraph">{{item.content}}</view>
    </view>

    <view class="section-title">联系方式</view>
    <view class="contact-row">
      <view class="paragraph contact-email">邮箱：{{meta.contactEmail}}</view>
      <button class="copy-btn" hover-class="copy-btn--hover" catchtap="onCopyEmail">复制</button>
    </view>
    <view class="paragraph">{{meta.serviceTime}}</view>
    <view class="report-entry" bindtap="onTapReport">进入问题反馈</view>
    <view class="updated">版本：v1.0.0 ｜ 更新时间：{{meta.updatedAt}}</view>
  </view>
</view>
